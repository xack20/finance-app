<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      No custom entries needed. The fake SMS provider used by CaptureServiceBackfillInstrumentedTest
      is now registered in-process via MockContentResolver (a manifest-declared provider is hosted in
      the test APK's own process, which lacks the Kotlin stdlib and crashes when queried).
    -->
</manifest>
